import React from 'react';
import MasterControl from '../MasterControl';

// Settings page wraps the existing MasterControl page
export default function AdminSettings() {
    return <MasterControl />;
}
